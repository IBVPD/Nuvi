security:
    encoders:
        NS\SentinelBundle\Entity\User: sha512

    role_hierarchy:
        ROLE_SITE:        [ ROLE_USER, ROLE_CAN_CREATE_CASE ]
        ROLE_LAB:         ROLE_USER
        ROLE_RRL_LAB:     ROLE_USER
        ROLE_COUNTRY:     ROLE_USER
        ROLE_REGION:      ROLE_USER
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: [ROLE_USER, ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

    providers:
        users:
            id: ns.sentinel.user_provider

    firewalls:
        dev:
            pattern:  ^/(_(profiler|wdt)|css|images|js)/
            security: false

        login:
            pattern:  ^/login$
            anonymous: ~

        secured_area:
            pattern:    ^/
            admin-login:
                check_path: /login_check
                login_path: /login
                default_target_path: /
                always_use_default_target_path: true
            logout:
                path:   /logout
                target: /
            switch_user: true

    access_control:
#        - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY, requires_channel: https }
        - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/_trans, roles: ROLE_USER }
        - { path: ^/admin, roles: ROLE_USER }
        - { path: '^/[^/]+/meningitis/edit', roles: ROLE_SITE }
        - { path: '^/[^/]+meningitis/create', roles: ROLE_SITE }
        - { path: '^/[^/]+meningitis/show', roles: [ ROLE_COUNTRY, ROLE_SITE, ROLE_LAB ] }
        - { path: ^/, roles: ROLE_USER }
