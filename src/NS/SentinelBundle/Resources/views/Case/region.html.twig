<table id="{{tableId}}" class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>{{'Case ID'|trans}}</th>
            <th>{{'Site Case ID'|trans}}</th>
            <th>{{'Admitted'|trans}}</th>
            <th>{{'Site'|trans}}</th>
            <th>{{'Country'|trans}}</th>
            <th class="hidden-480">{{'Status'|trans}}</th>
            <th>{{'Actions'|trans}}</th>
        </tr>
    </thead>
    <tbody>
        {%for row in results %}
            <tr>
                <td>{{ row.id }}</td>
                <td>{{ row.caseId}}</td>
                <td>{% if row.admDate %}{{ row.admDate|date('d/m/Y') }}{% endif %}</td>
                <td>{{ row.site }}</td>
                <td>{{ row.country }}</td>
                <td class='hidden-480'>
                    {{case_label(row,'EPI'|trans)}}
                    {{case_lab_label(row,'LAB'|trans)}}
                </td>
                <td>
                    <div class="visible-lg hidden-md hidden-sm hidden-xs btn-group">{{case_big_actions(row,false)}}</div>
                    <div class="visible-xs visible-sm visible-md hidden-lg">
                        <div class="inline position-relative">{{case_sm_actions(row)}}</div>
                    </div>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
